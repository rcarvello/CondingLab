# ✅ 08 - Funzioni stringa

## 🔹 Quesiti a risposta multipla

### **8.1 — Domanda 1**

Quale metodo restituisce una stringa completamente in MAIUSCOLO?
- A) `.upper()`
- B) `.caps()`
- C) `.big()`
- D) `.capitalize()`

---

### **8.2 — Domanda 2**

Cosa fa il metodo `.strip()`?
- A) Rimuove tutte le vocali
- B) Converte in minuscolo
- C) Rimuove gli spazi all’inizio e alla fine
- D) Divide la stringa in parti

---

### **8.3 — Domanda 3**

Dato il codice:

```python
s = "ciao mondo"
print(s.split())
```

Cosa verrà stampato?
- A) "ciao mondo"
- B) ["ciao\nmondo"]
- C) ["ciao", "mondo"]
- D) Errore

---

### **8.4 — Domanda 4**

Quale metodo sostituisce una parola con un’altra?
- A) `.switch()`
- B) `.replace()`
- C) `.edit()`
- D) `.change()`

---

## 🔹 Esercizi

### **8.5 — Esercizio 1**

Dati in input:

```
s = "Python Programmazione"
```

Stampare:

* tutta in minuscolo
* tutta in maiuscolo
* con la prima lettera maiuscola

---

### **8.6 — Esercizio 2**

Dato in input:

```
testo = "   ciao a tutti   "
```

Rimuovere gli spazi all’inizio e alla fine.

---

### **8.7 — Esercizio 3**

Dato in input:

```
frase = "Oggi è una bella giornata"
```

Dividere la frase in parole usando `.split()`.

---

### **8.8 — Esercizio 4**

Dato in input:

```
frase = "Mi piace Python"
```

Sostituire `"Python"` con `"Java"`.

---

### **8.9 — Esercizio 5**

Dato in input:

```
frase = "oggi imparo Python"
```

Stampare quante parole contiene (usando `.split()`).

---

### **8.10 — Esercizio 6**

Dato in input:

```
s = "Ciao, come stai?"
```

Stabilire se contiene la parola `"ciao"` (ignorando maiuscole/minuscole).

---


